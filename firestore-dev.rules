rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - Allow all authenticated users
    // ⚠️ WARNING: Change to RBAC rules before production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
